import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'react-firebase-chat',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
